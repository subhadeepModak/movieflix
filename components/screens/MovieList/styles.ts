import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: '#dadada',
    height: '100%',
    width: '100%',
  },
  indicatorContainer: {
    height: 50,
    display: 'flex',
    paddingVertical: 5,
    width: '100%',
    alignItems: 'center',
  },
});
